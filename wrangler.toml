name = "blogscale"
main = ".open-next/worker.js"
compatibility_date = "2024-11-01"
compatibility_flags = ["nodejs_compat"]

[assets]
directory = ".open-next/assets"
binding = "ASSETS"

# Environment variables should be managed as SECRETS in the Cloudflare Dashboard
# to prevent them from being overwritten during deployment.
